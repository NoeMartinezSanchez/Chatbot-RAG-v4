# API Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
python-multipart==0.0.6

# Utilities
requests==2.31.0
loguru==0.7.2
tqdm==4.66.1  # Para barras de progreso en descargas
tenacity==8.2.3  # Para reintentos en descargas

# ============================================
# ML Core - VERSIONES ACTUALIZADAS para TinyLlama
# ============================================

# PyTorch (necesario para cuantización 4-bit y Flash Attention)
torch==2.3.0
torchvision==0.18.0
torchaudio==2.3.0

# Hugging Face Transformers (última versión estable con soporte TinyLlama)
transformers==4.41.0
accelerate==0.31.0  # Esencial para carga optimizada en GPU/CPU
huggingface-hub==0.23.0
tokenizers==0.19.0
safetensors==0.4.3  # Seguro y rápido para guardar/cargar pesos

# Quantization 4-bit (bitsandbytes)
bitsandbytes==0.43.1  # PARA CUANTIZACIÓN 4-BIT (Windows/Linux)
# Para Windows, a veces se necesita: pip install bitsandbytes-windows
# O desde fuente: https://github.com/jllllll/bitsandbytes-windows

# Flash Attention (optimización para TinyLlama - opcional pero recomendado)
# flash-attn==2.5.0  # Solo si tienes GPU compatible

# Sentence Transformers (para embeddings - mantener versión reciente)
sentence-transformers==2.7.0
scikit-learn==1.4.0  # Para métricas de similitud

# Vector Search (mantener FAISS)
faiss-cpu>=1.8.0,<2.0.0
numpy==1.24.3

# Tokenization
tiktoken==0.5.1

# Memory Management (útil para monitorear uso de RAM/VRAM)
psutil==5.9.8
pynvml==11.5.0  # Para monitorear GPU NVIDIA

# Serialización (para guardar/recuperar índices)
joblib==1.3.2
pickle5==0.0.11  # Backport para pickle seguro

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.1

# Monitoring (opcional - para producción)
prometheus-client==0.19.0